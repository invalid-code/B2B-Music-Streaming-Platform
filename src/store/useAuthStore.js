// Store moved to frontend/src/store/useAuthStore.js
// Placeholder to prevent accidental imports from breaking during migration.

export default function useAuthStorePlaceholder() {
  return {
    user: null,
    token: null,
    login: () => {},
    logout: () => {},
  }
}
